# Ign1s Editor Workbench package

Open the tools from `Window > Editor Workbench` and configure decorations under `Project Settings > Editor Workbench`.

The package is Editor-only and relies on documented Unity Editor APIs. See the repository-level documentation for architecture and development workflow.
