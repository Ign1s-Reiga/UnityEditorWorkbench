# Sandbox Assets

This host project intentionally keeps product code out of `Assets`. The reusable implementation lives in the embedded package at `Packages/dev.ign1s-reiga.editor-workbench`.

Create temporary folders and scene objects here when manually testing Project Browser and Hierarchy behavior.
