# Sandbox Assets

This host project intentionally keeps product code out of `Assets`. The reusable implementation lives in the embedded package at `Packages/com.ign1s.editor-workbench`.

Create temporary folders and scene objects here when manually testing Project Browser and Hierarchy behavior.
